# RecruitFlow 🚀

**Интеллектуальная HR-платформа для автоматизации найма на базе ИИ.**

RecruitFlow — это современный инструмент для рекрутеров, который берет на себя рутину: от автоматического сбора откликов с почты до глубокого анализа собеседований.

---

## 📋 Содержание

- [Основные возможности](#-основные-возможности)
- [Технологический стек](#-технологический-стек)
- [Архитектура проекта](#-архитектура-проекта)
- [Установка и запуск](#-установка-и-запуск)
- [Настройка переменных окружения](#-настройка-переменных-окружения)
- [Интеграции](#-интеграции)
- [Развертывание](#-развертывание)
- [Структура проекта](#-структура-проекта)
- [Разработка](#-разработка)

---

## 🎯 Основные возможности

### 🤖 Автоматический скрининг (AI Screening)

* **Интеграция с Gmail**: Автоматический мониторинг входящих писем через IMAP
* **Классификация писем**: Определение резюме vs спам с помощью **Google Gemini 2.0 Flash**
* **Извлечение данных**: Автоматический парсинг структурированных данных из резюме:

  * ФИО, контакты (email, телефон, Telegram)
  * Опыт работы, навыки, стек технологий
  * Образование, языки, soft skills
  * Ожидаемая зарплата
* **Обработка вложений**: Поддержка PDF и DOCX резюме

### 🎙️ Анализ собеседований

* **Транскрибация**: Преобразование аудио в текст с использованием модели **GigaAM**
* **Диаризация**: Разделение речи по спикерам с помощью **pyannote.audio**
* **Обработка аудио**: Поддержка WAV, MP3 и других форматов через `pydub`
* **Хранение транскрипций**: Сохранение полных транскрипций интервью в базе данных

### 🗂️ Управление кандидатами

* **Канбан-доска**: Визуализация кандидатов по этапам:
  Новый → Скрининг → Интервью назначено → Интервью пройдено → Оффер → Отказ
* **Централизованное хранение**: Все данные в одном месте:

  * Резюме (PDF/DOCX)
  * Аудиозаписи интервью
  * Транскрипции
  * История взаимодействий

### 🏢 Проекты и Вакансии

* **Мультипроектность**: Создание отдельных проектов для разных команд
* **Управление вакансиями**: Настройка требований и параметров для каждой позиции
* **Импорт требований**: Автоматический импорт описания вакансии по URL

### 📅 Календарь и видеозвонки

* **Google Calendar**: Интеграция для определения свободных слотов
* **Zoom**: Автоматическое создание встреч через Zoom API

### 💬 Автоматические ответы кандидатам

* **Шаблонные письма**: Приглашения, отказы, напоминания и follow-up сообщения в один клик
* **Персонализация**: Динамическая подстановка данных кандидата
* **Отправка через Gmail API**: Надёжная доставка писем без вручную написанных писем

### 🤖 AI-собеседования через Telegram

* **Индивидуальный диалог-бот**: Кандидат проходит первичное интервью прямо в Telegram
* **Гибкие сценарии**: Настройка списка вопросов под конкретную вакансию
* **Анализ ответов**: AI оценивает ответы, выявляет сильные/слабые стороны
* **Автоматическая запись**: Все ответы, оценки и резюме диалога сохраняются в карточке кандидата

---

## 🛠 Технологический стек

### Backend
- **Python 3.10+** — основной язык программирования
- **Django 5.2.8** — веб-фреймворк
- **Gunicorn** — WSGI HTTP сервер для production
- **WhiteNoise** — обслуживание статических файлов

### База данных
- **PostgreSQL** — основная база данных
- **Redis** — кеширование и брокер сообщений для Celery

### Асинхронные задачи
- **Celery 5.5.3** — распределенная очередь задач
- **django-celery-beat 2.8.1** — планировщик периодических задач
- **Redis** — брокер сообщений

### AI & ML
- **Google Gemini 2.0 Flash** (`google-genai 1.51.0`) — LLM для анализа текста и классификации
- **GigaAM** — ASR модель для распознавания речи (транскрибация)
- **PyTorch 2.5.1** — фреймворк для ML моделей
- **pyannote.audio 3.3.1** — библиотека для диаризации речи
- **SpeechBrain 1.0.3** — инструментарий для обработки речи

### Обработка документов
- **pypdf 6.3.0** — парсинг PDF файлов
- **python-docx 1.2.0** — обработка DOCX файлов
- **BeautifulSoup4 4.14.2** — парсинг HTML контента

### Обработка аудио
- **pydub 0.25.1** — манипуляции с аудиофайлами
- **soundfile 0.13.1** — чтение/запись аудио файлов
- **torchaudio 2.5.1** — аудио обработка в PyTorch

### Интеграции
- **imap-tools 1.11.0** — работа с почтой через IMAP
- **google-auth 2.43.0** — OAuth2 для Google сервисов
- **requests 2.32.5** — HTTP клиент для API запросов
- **aiogram** - телеграм бот 

### Утилиты
- **python-dotenv 1.2.1** — загрузка переменных окружения
- **dj-database-url 3.0.1** — парсинг DATABASE_URL
- **Pydantic 2.12.4** — валидация данных

---

## 🏗 Архитектура проекта

```
RecruitFlow/
├── main/                    # Основное приложение
│   ├── models.py           # Модели данных (User, Project, Position, Candidate)
│   ├── views.py             # Представления (views)
│   ├── urls.py              # URL маршрутизация
│   ├── forms.py             # Django формы
│   ├── tasks.py             # Celery задачи
│   ├── services/            # Бизнес-логика
│   │   ├── llm_service.py          # Интеграция с Google Gemini
│   │   ├── asr_service.py          # Распознавание речи (GigaAM)
│   │   ├── diarization_service.py  # Диаризация речи
│   │   ├── audio_processing.py     # Обработка аудио
│   │   ├── mail_service.py         # Работа с почтой (IMAP)
│   │   ├── doc_reader_service.py   # Чтение PDF/DOCX
│   │   ├── parsing_servise.py      # Парсинг данных
│   │   ├── calendar_service.py     # Google Calendar API
│   │   └── zoom_service.py         # Zoom API
│   ├── repository/          # Слой доступа к данным
│   │   └── candidate.py
│   └── schemas/             # Pydantic схемы
│       └── llm_answers_schemas.py
├── RecruitFlow/             # Настройки проекта
│   ├── settings.py          # Конфигурация Django
│   ├── urls.py              # Главный URLconf
│   ├── celery.py            # Конфигурация Celery
│   ├── wsgi.py              # WSGI конфигурация
│   └── asgi.py              # ASGI конфигурация
├── templates/               # HTML шаблоны
├── static/                  # Статические файлы (CSS, JS)
├── media/                   # Загружаемые файлы (резюме, аудио)
├── requirements.txt         # Зависимости Python
├── Dockerfile               # Конфигурация Docker
├── fly.toml                 # Конфигурация Fly.io
└── manage.py                # Django CLI утилита
```

---

## 🚀 Установка и запуск

### Предварительные требования

- **Python 3.10+** (рекомендуется 3.12+)
- **PostgreSQL 12+**
- **Redis 6+**
- **Git** (для установки GigaAM из репозитория)
- **FFmpeg** (для обработки аудио, устанавливается автоматически в Docker)

### 1. Клонирование репозитория

```bash
git clone https://github.com/IslamJenishbekov/RecruitFlow.git
cd RecruitFlow
```

### 2. Создание виртуального окружения

**Linux/macOS:**
```bash
python -m venv venv
source venv/bin/activate
```

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

> **Примечание:** Установка может занять некоторое время, так как включает в себя:
> - PyTorch с зависимостями (~2-3 GB)
> - ML модели и библиотеки
> - GigaAM из GitHub репозитория

> **Для GPU (CUDA):** Если у вас есть NVIDIA GPU, вы можете установить PyTorch с поддержкой CUDA отдельно перед установкой requirements.txt:
> ```bash
> pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
> ```

### 4. Настройка базы данных

**Создание базы данных PostgreSQL:**
```sql
CREATE DATABASE recruitflow_db;
CREATE USER recruitflow_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE recruitflow_db TO recruitflow_user;
```

### 5. Настройка Redis

Убедитесь, что Redis запущен:
```bash
# Linux/macOS
redis-server

# Windows (если установлен через WSL или отдельно)
# Или используйте Docker:
docker run -d -p 6379:6379 redis:alpine
```

### 6. Применение миграций

```bash
python manage.py migrate
```

### 7. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

---

## ⚙️ Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Django
DJANGO_SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# База данных
DATABASE_URL=postgresql://recruitflow_user:your_password@localhost:5432/recruitflow_db

# Google Gemini API
GOOGLE_API_KEY=your-google-gemini-api-key

# Redis (если отличается от дефолтного)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=1

# Email (опционально, если не используете App Password пользователя)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Media files (опционально, для production)
MEDIA_ROOT=/path/to/media
STATIC_ROOT=/path/to/staticfiles
```

### Получение API ключей

1. **Google Gemini API Key:**
   - Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
   - Создайте новый API ключ
   - Скопируйте ключ в `GOOGLE_API_KEY`

2. **Gmail App Password:**
   - Перейдите в [Настройки аккаунта Google](https://myaccount.google.com/)
   - Включите двухфакторную аутентификацию
   - Создайте "Пароль приложения" для почты
   - Используйте этот пароль в профиле пользователя в системе

3. **Zoom API (опционально):**
   - Создайте приложение в [Zoom Marketplace](https://marketplace.zoom.us/)
   - Получите Account ID, Client ID и Client Secret
   - Добавьте их в профиль пользователя через веб-интерфейс

---

## 🔌 Интеграции

### Gmail (IMAP)
- Автоматическая проверка входящих писем каждые 5 минут
- Извлечение резюме из писем и вложений
- Классификация через Gemini AI

### Google Calendar
- OAuth2 авторизация через Google
- Поиск свободных слотов в календаре
- Создание событий для интервью

### Zoom
- Создание видеоконференций через Zoom API
- Автоматическая генерация ссылок на встречи
- Интеграция с календарем

---

## 🚢 Развертывание

### Docker

Проект включает `Dockerfile` для контейнеризации:

```bash
# Сборка образа
docker build -t recruitflow .

# Запуск контейнера
docker run -d \
  -p 8000:8000 \
  -e DATABASE_URL=postgresql://... \
  -e GOOGLE_API_KEY=... \
  -v $(pwd)/media:/app/media \
  recruitflow
```

### Fly.io

Проект настроен для развертывания на Fly.io:

1. Установите [Fly CLI](https://fly.io/docs/getting-started/installing-flyctl/)
2. Авторизуйтесь: `fly auth login`
3. Создайте приложение: `fly launch`
4. Настройте переменные окружения через `fly secrets set`
5. Разверните: `fly deploy`

Конфигурация находится в `fly.toml`.

---

## 📁 Структура проекта

### Модели данных

- **CustomUser**: Расширенная модель пользователя с настройками интеграций
- **Project**: Проекты/команды
- **Position**: Вакансии в рамках проектов
- **Candidate**: Кандидаты с полной информацией и файлами

### Сервисы

- **LLMService**: Работа с Google Gemini для анализа текста
- **ASRService**: Распознавание речи через GigaAM
- **DiarizationService**: Разделение речи по спикерам
- **MailService**: Мониторинг и обработка почты
- **DocumentReader**: Извлечение текста из PDF/DOCX
- **ParsingService**: Парсинг структурированных данных
- **CalendarService**: Интеграция с Google Calendar
- **ZoomService**: Создание Zoom встреч

### Celery задачи

- **check_email_task**: Периодическая проверка почты (каждые 5 минут)

---

## 💻 Разработка

### Запуск в режиме разработки

1. **Django сервер:**
```bash
python manage.py runserver
```

2. **Celery Worker** (в отдельном терминале):
```bash
# Linux/macOS
celery -A RecruitFlow worker -l info

# Windows
celery -A RecruitFlow worker -l info -P solo
```

3. **Celery Beat** (для периодических задач, в отдельном терминале):
```bash
celery -A RecruitFlow beat -l info
```

### Сбор статических файлов

```bash
python manage.py collectstatic --noinput
```

### Логирование

Логи сохраняются в:
- Консоль (INFO уровень)
- `logs/django.log` (ротация при 5MB, до 5 файлов)

### Тестирование

```bash
python manage.py test
```

---

## 🐛 Решение проблем

### Проблемы с GigaAM

Если возникают ошибки при загрузке модели:
- Убедитесь, что у вас достаточно RAM (рекомендуется 4GB+)
- Проверьте наличие интернет-соединения для загрузки моделей
- Для GPU: убедитесь, что CUDA установлена корректно

### Проблемы с Celery на Windows

Используйте флаг `-P solo`:
```bash
celery -A RecruitFlow worker -l info -P solo
```

### Проблемы с базой данных

Убедитесь, что PostgreSQL запущен и доступен:
```bash
# Проверка подключения
psql -h localhost -U recruitflow_user -d recruitflow_db
```

### Проблемы с Redis

Проверьте, что Redis запущен:
```bash
redis-cli ping
# Должен вернуть: PONG
```

---

## 📝 Лицензия

Проект находится в стадии активной разработки (MVP).

---

## 🤝 Вклад в проект

Будем рады вашим Pull Request'ам! Перед отправкой:

1. Создайте форк проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📧 Контакты

Проект: [RecruitFlow](https://github.com/IslamJenishbekov/RecruitFlow)

---

**Сделано с ❤️ для автоматизации HR процессов**
