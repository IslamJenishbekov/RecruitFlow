# RecruitFlow 🚀

**Интеллектуальная HR-платформа для автоматизации найма на базе ИИ.**

RecruitFlow — это современный инструмент для рекрутеров, который берет на себя рутину: от автоматического сбора откликов с почты до глубокого анализа собеседований.

## 🎯 Основные возможности

*   **🤖 Автоматический скрининг (AI Screening):**
    *   Интеграция с Gmail для мониторинга входящих писем.
    *   Классификация писем (Резюме / Спам) с помощью **Google Gemini**.
    *   Извлечение структурированных данных (ФИО, навыки, опыт, контакты) из текста резюме и вложений.
*   **🎙️ Анализ собеседований:**
    *   Загрузка аудиозаписей интервью.
    *   Транскрибация речи в текст с использованием модели **GigaAM**.
    *   (В планах) Диаризация и выделение ключевых инсайтов.
*   **🗂️ Управление кандидатами:**
    *   Канбан-доска или списки по этапам (Новый -> Скрининг -> Интервью -> Оффер).
    *   Хранение истории взаимодействия, резюме и транскрибаций в одном месте.
*   **🏢 Проекты и Вакансии:**
    *   Создание проектов под разные команды.
    *   Настройка требований для каждой вакансии (можно импортировать по URL).

## 🛠 Технологический стек

*   **Backend:** Python 3.12+, Django 5.2
*   **Database:** PostgreSQL
*   **Async Tasks:** Celery + Redis
*   **AI & ML:**
    *   **LLM:** Google Gemini (`google-genai`) — анализ текста.
    *   **ASR:** GigaAM (`gigaam`) — распознавание речи.
    *   **Audio:** `pyannote-audio`.

## 🚀 Установка и запуск

### Предварительные требования
*   Python 3.10+
*   PostgreSQL
*   Redis (для Celery)

### 1. Клонирование репозитория
```bash
git clone https://github.com/IslamJenishbekov/RecruitFlow.git
cd RecruitFlow
```

### 2. Создание виртуального окружения
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```
*Примечание: Для работы `gigaam` и `torch` может потребоваться отдельная установка с поддержкой CUDA, если есть GPU.*

### 4. Настройка переменных окружения
Создайте файл `.env` в корне проекта и добавьте:

```env
SECRET_KEY=your_django_secret_key
DEBUG=True
DATABASE_URL=postgres://user:password@localhost:5432/recruitflow_db
GOOGLE_API_KEY=your_gemini_api_key
# Настройки почты (если используются не через App Password пользователя)
# EMAIL_HOST_USER=...
# EMAIL_HOST_PASSWORD=...
```

### 5. Применение миграций
```bash
python manage.py migrate
```

### 6. Запуск

**Запуск Django сервера:**
```bash
python manage.py runserver
```

**Запуск Celery (для фоновых задач):**
```bash
# Linux/macOS
celery -A RecruitFlow worker -l info

# Windows (рекомендуется режим pool=solo для разработки)
celery -A RecruitFlow worker -l info -P solo
```

**Запуск Celery Beat (для периодических задач):**
```bash
celery -A RecruitFlow beat -l info
```

## 🤝 Вклад в проект
Проект находится в стадии активной разработки (MVP). Будем рады вашим Pull Request'ам!